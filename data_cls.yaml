# 1단계: 분류 모델용 데이터셋 설정
# 폴더 구조:
#   dataset/train/reusable/, dataset/train/recycle/, dataset/train/dispose/
#   dataset/val/...
#   dataset/test/...

path: /workspace/dataset/classification

train: train
val:   val
test:  test

nc: 3
names:
  0: reusable   # 재사용
  1: recycle    # 재활용
  2: dispose    # 폐기
